IF &NUMERO
ELSE
 SET &NUMERO = $NUMERO
ENDIF
SET &OUTIL_COURANT = &NUMERO
IF &NUMERO
ELSE
 SET &NUMERO = $NUMERO
ENDIF
SET &OUTIL_COURANT = &NUMERO
SETOPER &FACTOR = $TILDE_33 / $TILDE_R
IF &FACTOR < 1.0
SETOPER &MY_CUTFEEDRATE = 0.75 * &FACTOR * &TP_CUT_FEEDRATE
ELSE
SETOPER &MY_CUTFEEDRATE = &TP_CUT_FEEDRATE
ENDIF
~h~i I~"37" J~"38" F&MY_CUTFEEDRATE/.0/
F&TP_CUT_FEEDRATE/.0/

